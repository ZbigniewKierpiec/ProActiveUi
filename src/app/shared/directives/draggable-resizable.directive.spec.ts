import { DraggableResizableDirective } from './draggable-resizable.directive';

describe('DraggableResizableDirective', () => {
  it('should create an instance', () => {
    const directive = new DraggableResizableDirective();
    expect(directive).toBeTruthy();
  });
});
